Template Driven Forms:

(ngSubmit)="onSubmit()" #formref="ngForm"

FormControl:
#controlname="ngModel"

FormGroupControl:

#groupcontrolname="ngModelGroup"

this.form.reset();
Angular Pipes:

Parameterized pipes
Custom pipes
Impure Pipes
Custom pipes should implement PipeTransform, add a @Pipe() decorator
